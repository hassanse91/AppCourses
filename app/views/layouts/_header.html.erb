  <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Snapshot Project", root_path, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right">
            <% if !admin_signed_in? %>
              <%= link_to "Sign in", new_admin_session_path, class: "btn btn-success" %>
            <%= link_to "Sign up", new_admin_registration_path, class: "btn btn-default" %>
            <% else %>
           <%= link_to "New Artcile", new_article_path, class: "btn btn-success" %>
          <%= link_to "Account", edit_admin_registration_path, class: "btn btn-default" %>
            <%= link_to "deconnection", destroy_admin_session_path, class: "btn btn-default" %>
            <% end %>
          </form>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>